{
  "name": "harbourRWA-contracts",
  "version": "1.0.0",
  "description": "HarbourRWA smart contracts",
  "type": "commonjs",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network ethereumSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network ethereumMainnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "hh2",
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^16.0.0",
    "hardhat": "^3.13.0",
    "typescript": "^5.0.0"
  }
}
